# DECISIONS.md — 架构决策记录

所有标记为 FINAL 的决策不得在不更新本文件的前提下推翻。

> ⚠️ **关于旧版实现**：部分决策（D010–D019）引用了旧版代码中的具体实现或 spec 内容，这些旧版代码均已删除或标记为参考材料，不保证与新版 CRE 需求兼容。标注"旧版参考"的决策需在新版开发时重新验证后才能升格为 FINAL。

---

| ID | 范围 | 决策 | 原因 | 状态 |
|----|------|------|------|------|
| D001 | 数据格式 | 使用 TorchRL `TensorDict` 作为 env/policy/collector 边界的统一数据容器 | Isaac Sim 集成要求 TorchRL；`gym.Env` 范式与向量化 Isaac 环境不兼容 | FINAL |
| D002 | Policy | PPO 实现为 `TensorDictModuleBase` 子类 | 直接支持 TensorDict I/O，无需手动提取张量；`SyncDataCollector` 兼容性要求 | FINAL |
| D003 | Done 编码 | `done_type` 为多通道整数：0=running, 1=success, 2=collision, 3=out_of_bounds, 4=truncated | 单 bool `done` 丢失终止原因；CRE 分析需要分类统计 | FINAL |
| D004 | 安全阈值 | `near_violation_distance = 0.5m` 用于近距违规检测 | 匹配无人机物理尺寸 + 安全余量；新版 spec 定义后需重新确认是否沿用 | 旧版参考，待确认 |
| D005 | 成功判定 | episode 成功 = 终止时 `goal_distance < 0.5m`（即 `done_type == 1`） | 与物理停靠容差匹配；新版 spec 定义后需重新确认 | 旧版参考，待确认 |
| D006 | 配置管理 | 所有训练/eval/audit 配置走 Hydra（`cfg/`） | 可组合配置、支持 sweep、保证可复现性 | FINAL |
| D007 | 实验跟踪 | WandB 记录训练指标；CRE 分析结果另行决定如何记录 | 集中可见性；CRE 结果记录方式待 Phase 8（Integration）设计时决定 | FINAL（WandB 部分）；CRE 记录方式待定 |
| D008 | Spec 版本控制 | 新版 spec 文件从 `*_v1.yaml` 开始命名（旧版 v0 已删除）；后续变更创建更高版本，禁止原地修改 | 旧版 `cfg/spec_cfg/` 已删除；新版需根据 CRE_v4.pdf 重新设计格式 | FINAL |
| D009 | 新包位置 | `analyzers/`、`repair/`、`pipeline/` 等新包均在 `isaac-training/training/` 下创建，与 `envs/`、`runtime_logging/` 同级 | 与现有包结构保持一致；具体包结构待阅读 CRE_v4.pdf 后确定 | FINAL |
| D010 | Reward 组件 | 旧版记录 6 个组件：`reward_progress`、`reward_safety_static`、`reward_safety_dynamic`、`penalty_smooth`、`penalty_height`、`manual_control` | 旧版 spec 已删除；新版组件定义需根据 CRE_v4.pdf 重新确定 | 旧版参考，待重新定义 |
| D011 | Constraint 定义 | 旧版记录 5 个约束：`collision_avoidance`、`safety_margin`、`workspace_boundary`、`speed_bound`、`attitude_turn_rate` | 旧版 spec 已删除；新版约束定义需根据 CRE_v4.pdf 重新确定 | 旧版参考，待重新定义 |
| D012 | CRE 日志单元 | 旧版用 `FlightEpisodeLogger` 作为 per-episode 日志写入器 | 旧版设计思路；新版日志架构需根据 CRE_v4.pdf 重新设计 | 旧版参考，待重新设计 |
| D013 | TensorDict 适配器 | 旧版用 `TrainingRolloutLogger` 桥接 TensorDict batch 到日志器 | 旧版设计思路；新版适配器需与新版 CRE 日志格式对齐 | 旧版参考，待重新设计 |
| D014 | 日志聚合 | 旧版用 `aggregate_log_directory(run_dir)` 计算运行级指标 | 旧版设计思路；新版聚合逻辑待定 | 旧版参考，待重新设计 |
| D015 | Acceptance Check | 旧版用 `run_acceptance_check(run_dir)` 作为 Phase 门控 | 旧版设计思路；新版 acceptance 规则需与新版分析结果对齐 | 旧版参考，待重新设计 |
| D016 | Success rate | 旧版 `cre_logging.py::aggregate_episode_records()` 比 `baseline_runner.py::summarize_rollout_metrics()` 更接近真值 | 两者均为旧版；新版需根据新 spec 重新定义 success 判定 | 旧版参考，待重新定义 |
| D-S1 | Phase 1 / Reward Spec | `reward_spec_v1.yaml` 采用 weighted DAG：顶层必填 `spec_type`、`spec_version`、`reward_terms[]`、`dag_edges[]`；每个 term 必填 `term_id:str`、`term_expr:str|dict`、`weight:float`（单位 `unitless`）、`unit:str`（描述 term 输出与 `clip_bounds` 的单位）、`clip_bounds:{min:float|null,max:float|null}`、`shaping_flag:bool`；`dag_edges` 为必填列表，元素 `{from:str,to:str}`，允许空列表表示无 shaping 依赖 | 依据 CRE_v4 Part I §2.2，reward 必须表示为带权 DAG，节点携带 `weight` / `clip_bounds` / `shaping_flag`，边编码 shaping 依赖；本项目额外保留 `unit` 元数据以满足 Phase 1 DoD 的“单位”要求 | FINAL |
| D-S2 | Phase 1 / Constraint Spec | `constraint_spec_v1.yaml` 顶层必填 `spec_type`、`spec_version`、`constraints[]`；每条约束必填 `constraint_id:str`、`indicator_predicate:str|dict`、`severity:enum{hard,soft,info}`、`temporal_scope:enum{instantaneous,episodic,cumulative}`、`coverage_threshold_delta:float>0`；字段 `tolerance:float|null` 与 `penalty_weight:float|null` 在所有约束上保留，`severity=="soft"` 时必须为非空，`hard/info` 时写 `null` | Def 1.2 与 Part I §2.2 要求约束包含谓词、严重等级、时间范围；Part II Core Data Schemas 进一步显式给出 `zeta` / `lam` / `delta` typed fields，本项目直接以 YAML 固化这些字段而不经 LLM-α 填充 | FINAL |
| D-S3 | Phase 1 / Policy Spec | `policy_spec_v1.yaml` 顶层必填 `spec_type`、`spec_version`、`action_space`、`observation_space`、`execution_frequency_hz`；`action_space` 必填 `tensor_key`、`shape`、`dtype`、`bounds`、`unit`；`observation_space` 为条目列表，每项必填 `key`、`shape`、`dtype`、`unit`；`execution_frequency_hz` 类型为 `float`，单位 `Hz` | CRE_v4 将 `Pi` 表示为动作空间、观测空间、执行频率；`INTERFACES.md` 已给出当前 TorchRL action / observation key 约束，足以冻结 spec 格式，但本决策不在此锁定具体频率数值 | FINAL |
| D-S4-env | Phase 1 / Environment Spec | 不直接复用 `cfg/env_cfg/*.yaml` 作为 CRE environment spec；下一步新建 `cfg/spec_cfg/env_spec_v1.yaml` 作为 CRE manifest，并通过 `env_cfg_refs` 指向现有 `env_cfg` 文件。该 manifest 必须显式表达 `E_tr` / `E_dep`、`scene_families[]`、`shift_operators[]`、`generator_seeds[]` | CRE_v4 Part I §2.2 要求 environment 表示同时包含 scene families、shift operators、generator seeds。现有 `env_cfg` 仅分散提供单个 `scene_family` 与单个 `seed`，并未显式编码 shift operator，也未声明训练分布与部署集合之间的关系，因此只能作为底层场景生成配置，不能直接充当完整 spec | FINAL |
| D-LLM-α | Phase 1 / Stage I 偏差 | 历史上曾将 CRE_v4 Stage I 的 LLM-α `NLInput -> SpecS` 自然语言解析层裁掉，改为人工编写结构化 YAML spec；该路线当前仅保留为 compatibility / offline fallback 语义，不再代表 canonical path。canonical M1 已由 `D-I21` 恢复为 `parse_nl_input` + ambiguity / escalation 链路 | 该历史偏差曾用于缩小实现范围，但已被用户明确否决为 canonical 架构。保留该记录仅用于解释历史代码与文档为何出现 YAML-only 路线，不得再据此否定 PDF 中的 LLM-α 主链路 | FINAL（historical / compatibility-only；canonical scope superseded by D-I21） |
| D-I10 | Phase 0 / SpecS Interface | `SpecS(spec_id: str, E_tr: TrainingEnvDistribution, E_dep: list[Environment], R: RewardDAG, C: list[Constraint], Pi: PolicyClass, version: int = 0)` 签名冻结；`SpecS` 必须为 `@dataclass(frozen=True)`，且 repair 流程只能返回新的 `SpecS` 对象，不得原地修改。初始冻结时未一并冻结 `NLInput` / `AmbiguityFlag`；其中 `NLInput` 的后续处理已由 `D-I11` 覆盖，`AmbiguityFlag` 的后续处理由 `D-I14` 覆盖 | Phase 0 Step 0.1 需要一个可依赖的 canonical schema 入口，供 `diag_report.py` 与合约测试先行落地。先冻结 `SpecS` 能解锁 Phase 0 数据结构工作；后续若需把其他 Part II core schema 重新纳入冻结范围，需通过独立决策逐项恢复，避免把已放弃的 LLM-α 运行时语义整体回灌进接口契约 | FINAL |
| D-I11 | Phase 0 / NLInput Interface | `NLInput(r_desc: str, c_desc: str, e_desc: str, metadata: dict[str, Any])` 签名冻结，并以 `@dataclass(frozen=True)` 的 passive schema 形式保留在 `analyzers/diag_report.py` 中；`r_desc` / `c_desc` / `e_desc` 对应 PDF Part II §1.2 的 reward / constraint / environment 原始描述，`metadata` 为 free-form 透传字典。该决策冻结的是 Phase 0 的 canonical 数据结构本身；Stage I 的运行时 `NLInput -> SpecS` 链路现由 `D-I21` 恢复并接管 | `D-LLM-α` 放弃的是运行时 `NLInput -> SpecS` 解析流程，不等于删除 Part II §1.2 中的 `NLInput` 数据结构。当前 `TRACEABILITY.md` 已将 `NLInput` 列为 Phase 0 追溯项，若接口持续 `[PENDING]` 会导致 Phase 0 顺序无法推进。因此本决策先将 `NLInput` 重新纳入 Phase 0 冻结范围；其 runtime 语义已在 strict PDF correction 中由 `D-I21` 明确恢复 | FINAL |
| D-I12 | Phase 0 / Constraint Interface | `Constraint(k_id: str, predicate: Callable[[State, Action], int], sigma: Literal["hard","soft","info"], scope: Literal["instantaneous","episodic","cumulative"], zeta: float | None, lam: float | None, delta: float)` 签名冻结，并以 `@dataclass(frozen=True)` 的 canonical schema 形式保留在 `analyzers/diag_report.py` 中。该接口独立于 `SpecS.C` 被冻结，后续 `SpecS`、`DiagReport`、M2/M4/M7/M8 只能依赖这一运行时 dataclass 接口 | `D-S2` 冻结的是 `constraint_spec_v1.yaml` 的 YAML 合同；它解决的是配置文件字段设计，不等于冻结运行时 canonical schema。`Constraint` 作为 Part II §1.2 p.22 的 core data structure，需要像 `NLInput` 一样拥有独立接口条目，否则 `TRACEABILITY.md`、`INTERFACES.md` 与 `diag_report.py` 会持续失配。当前 `diag_report.py` 已存在同签名实现，因此本决策主要用于完成接口冻结与跨文件对齐 | FINAL |
| D-I13 | Phase 0 / RewardDAG Interface | `RewardDAG(nodes: list[RewardTerm], edges: list[tuple[str, str]])` 签名冻结，并以 `@dataclass(frozen=True)` 的 canonical schema 形式保留在 `analyzers/diag_report.py` 中。该接口独立于 `SpecS.R` 被冻结，后续 `SpecS`、`DiagReport`、M2/M5/M7 只能依赖这一运行时 dataclass 接口 | Reward YAML/spec 决策冻结的是配置格式与字段来源，不等于冻结运行时 canonical schema。`RewardDAG` 作为 Part II §1.2 p.22 的 core data structure，需要像 `NLInput`、`Constraint` 一样拥有独立接口条目，否则 `TRACEABILITY.md`、`INTERFACES.md` 与 `diag_report.py` 会持续失配。当前 `diag_report.py` 已存在同签名实现，因此本决策主要用于完成接口冻结与跨文件对齐 | FINAL |
| D-I14 | Phase 0 / AmbiguityFlag Interface | `AmbiguityFlag(flag_type: Literal["UNDEFINED_THRESHOLD","MISSING_ENV_SCOPE","REWARD_UNDERSPECIFIED","CONSTRAINT_OVERLAP","TEMPORAL_AMBIGUITY"], location: str, impact_score: float)` 签名冻结，并以 `@dataclass(frozen=True)` 的 passive canonical schema 形式保留在 `analyzers/diag_report.py` 中；`flag_type` 仅允许 PDF Part II §1.2 p.22 给出的五个枚举值，`location` 为 `SpecS` 内的 dot-separated JSON path，`impact_score` 必须落在 `[0,1]`，表示估计的 `|DeltaPsiCRE|` reduction。该决策冻结的是 Phase 0 的 canonical schema 本身；Stage I 的 ambiguity generation / escalation 运行时链路现由 `D-I21` 恢复并接管 | `D-LLM-α` 放弃的是运行时 ambiguity flag 生成流程，不等于删除 Part II §1.2 中的 `AmbiguityFlag` canonical schema。当前 `TRACEABILITY.md` 已将 `AmbiguityFlag` 列为 Phase 0 追溯项，而 `INTERFACES.md` 若继续保留 `[PENDING]` 会阻塞 P2 与后续合约测试。因此本决策先将 `AmbiguityFlag` 重新纳入 Phase 0 冻结范围；其 runtime 语义已在 strict PDF correction 中由 `D-I21` 明确恢复 | FINAL |
| D-I15 | Phase 0 / DiagReport Interface | `DiagReport(spec_id: str, timestamp: str, phi_cr2: float, phi_ec_bar: float, phi_er3: float, phi_cr1: float | None, phi_ec2: float | None, phi_ec3: float | None, phi_ec_per_j: list[float] | None, phi_er1: float | None, phi_er2: float | None, kappa_cr: float | None, gamma_ec: float | None, delta_er: float | None, psi_cre: float, ci_95: dict[str, tuple[float, float]], flags: list[str], discrepancy: list[DiscrepancyRecord] | None, failure_hypothesis: str | None, repair_targets: list[str] | None)` 签名冻结，并以 `@dataclass(frozen=True)` 的 canonical report schema 形式保留在 `analyzers/diag_report.py` 中；其依赖类型 `DiscrepancyRecord` 在当前 PDF 摘录中只以类型名出现，因此同文件先冻结为 passive placeholder，供 `DiagReport.discrepancy` 引用 | `DiagReport` 是 PDF Part II §1.2 pp.22–23 规定的跨阶段统一报告载体；如果在 Phase 0 不先冻结其完整字段布局，后续 M2/M3/M4/M7/M8 很容易各自引入替代报告结构，重演历史审计中“自创结构替代 DiagReport”的偏差。即使 `discrepancy`、`failure_hypothesis`、`repair_targets` 分别由 Stage III / M4 等后续阶段填充，它们仍是 canonical schema 的一部分，必须先在接口层保留，而不是等到对应算法实现时再临时扩展 | FINAL |
| D-I16 | Phase 0 / RepairProposal Interface | `RepairProposal(proposal_id: str, spec_prime: SpecS, operator_class: str, declared_side_effects: dict[str, str], semantic_justification: str, predicted_delta_psi: float | None, rough_delta_psi: float | None)` 签名冻结，并以 `@dataclass(frozen=True)` 的 canonical repair proposal schema 形式保留在 `analyzers/diag_report.py` 中；`proposal_id` 为 UUID 字符串，`spec_prime` 必须依赖已冻结的 `SpecS`，`operator_class` 来自 `V_R` vocabulary，`declared_side_effects` 为 `dim -> description` 的字符串映射，`semantic_justification` 上限 200 tokens，`predicted_delta_psi` / `rough_delta_psi` 在对应排名阶段前可为 `None` | 为满足 Phase 0 Step 0.1 的 P2 前置条件，`RepairProposal` 必须在实现与合约测试开始前先完成接口冻结。PDF Part II §1.2 p.23 已给出完整字段集合，当前 `TRACEABILITY.md` 与 `STATUS.md` 也都将其标记为下一个待实现结构；若继续缺失该冻结条目，Phase 0 将无法合法推进到 `RepairProposal` 的合约测试与字段级校验 | FINAL |
| D-I17 | Phase 0 / AcceptanceVerdict Interface | `AcceptanceVerdict(accepted: bool, c1_pass: bool, c2_pass: bool, c3_pass: bool, c4_pass: bool, s_sem: float, intent_preserved: bool, rejection_feedback: str | None)` 签名冻结，并以 `@dataclass(frozen=True)` 的 canonical acceptance verdict schema 形式保留在 `analyzers/diag_report.py` 中；`accepted`、`c1_pass`、`c2_pass`、`c3_pass`、`c4_pass`、`intent_preserved` 均为布尔字段，`s_sem` 必须落在 `[0,1]`，`rejection_feedback` 为 `str | None`，用于 rejection 场景反馈 | 为满足 Phase 0 Step 0.1 的 P2 前置条件，`AcceptanceVerdict` 必须在实现与合约测试开始前先完成接口冻结。PDF Part II §1.2 p.23 已给出完整字段集合，当前 `TRACEABILITY.md` 与 `STATUS.md` 也都将其标记为下一个待实现结构；若继续缺失该冻结条目，Phase 0 将无法合法推进到 `AcceptanceVerdict` 的合约测试与字段级校验 | FINAL |
| D-I18 | Phase 0 / CFG Interface | `CFG` 作为 Part II §1.3 Table 4 pp.23–24 定义的模块级 singleton 冻结，导出类型为 `CFGSchema`：`CFGSchema(eta_flag: float = 0.15, llm_retry_limit: int = 3, delta_j_default: float = 0.50, n_ref_traj: int = 500, n_mc_kj: int = 1000, critical_region_radius: float = 0.10, ec_aggregation_mode: Literal["mean","min"] = "mean", eps_stab: float = 1e-6, enhanced_estimators_enabled: bool = True, eta_disc: float = 0.15, tau_low: float = 0.30, tau_high: float = 0.70, lambda_expand: float = 0.50, n_rechk: int = 1, f_steepness: float = 8.0, f_inflection: float = 0.50, w_cr: float = 0.333, w_ec: float = 0.333, w_er: float = 0.334, b_bootstrap: int = 1000, k_detect: float = 1.5, k_alarm: float = 3.0, tau_alarm_psi: float = 0.75, k_proposals: int = 3, eta_edit: float = 0.20, n_rank_episodes: int = 50, eps_rank: float = 0.02, ranking_mode: str = "rollout_first", tau_sem: float = 0.80, eps_perf: float = 0.05, n_max_iterations: int = 5)`；后续 M5 与 `pipeline/orchestrator.py` 只能复用这一 singleton，不得再创建第二套配置结构；权重约束为 `abs(w_cr + w_ec + w_er - 1.0) < 1e-9` 且三者均严格大于 0 | Phase 0 Step 0.2 需要先冻结全局配置字段布局，才能合法编写 `analyzers/cfg.py`、CFG 合约测试，以及后续 M5 / orchestrator 对 Table 4 常量的依赖。PDF 已明确 `CFG` 为 pipeline start 时加载的 singleton，并要求权重和校验失败时在任何模块运行前抛出 `CONFIG_WEIGHT_SUM_ERROR`；在 `analyzers/errors.py` 完成前，`cfg.py` 可先保留同名错误入口或占位常量，但接口语义此刻必须先冻结 | FINAL |
| D-I19 | Phase 0 / CREError Interface | `CREError` 作为 Part II §11 pp.49–51 定义的统一错误基类冻结，错误码命名空间由 `CREErrorCode` 承担，且 Table 5 中每个 code 都必须以 `CREErrorCode.CODE_NAME` 形式可访问；正式错误体系导出 `ErrorDescriptor` 与 `ERROR_REGISTRY` 作为 canonical registry surface，并至少包含 `ConfigWeightSumError(CREError)` 这样的错误子类绑定唯一 `error_code=CREErrorCode.CONFIG_WEIGHT_SUM_ERROR`。后续 `cfg.py`、`pipeline/orchestrator.py` 及各模块都必须复用这一错误体系，不得再散落字符串占位错误或自创错误码格式 | Phase 0 Step 0.3 需要先冻结错误体系接口，才能合法编写 `analyzers/errors.py`、错误码合约测试，以及把 `cfg.py` 中当前的 `ValueError("CONFIG_WEIGHT_SUM_ERROR")` 过渡路径接到正式错误类型。PDF §11 已明确 Table 5 是 complete registry，且 codes 需作为字符串常量暴露；如果不先冻结这一接口，后续模块很容易各自发明错误类和错误码命名，重新造成跨模块错误面漂移 | FINAL |
| D-I20 | Phase 1 / M1 YAML Parser 接口 | `analyzers/m1.py::parse_yaml_input()` 保留为 compatibility adapter：它可以把仓库现有四份 YAML spec 组织成 `NLInput` 或 canonical M1 所需的中间输入，但不得再作为 `TRACEABILITY.md` 中 `M1.parse_nl_input` 的实现函数，也不得充当 canonical LLM-α path。`analyzers/spec_validator.py` 继续承担 schema 验证与基础一致性检查 | 仓库已经积累了 Hydra/YAML spec 与离线测试资产，完全删除 YAML adapter 会增加迁移成本；但用户已明确要求严格回归 PDF，因此该适配层只能降级为 compatibility / offline fallback，而不能继续被认定为 Phase 1 的 canonical 完成项 | FINAL（compatibility-only；canonical M1 superseded by D-I21） |
| D-I21 | Phase 1 / 4 / 5 / 6 Strict PDF LLM Canonical Path | 恢复 CRE_v4 Part II 的 canonical LLM architecture：M1 使用 `LLM-α` 执行 `parse_nl_input(NLInput -> SpecS)` 与 ambiguity escalation；M4 使用 `LLM-β` 生成 `failure_hypothesis` 与 `repair_targets`；M7 使用 `LLM-δ` 生成 `RepairProposal` 候选；M8 使用 `LLM-ε` 执行 semantic consistency verification。`parse_yaml_input`、rule fallback 与其他离线路径只可作为 compatibility / auxiliary layer，不得再替代 canonical path。跨模块 canonical structures 仍然只有 `SpecS`、`DiagReport`、`RepairProposal`、`AcceptanceVerdict` | 用户已明确要求严格按 `doc/CRE_v4.pdf` 执行，并指出“LLM 是整个架构中最重要的一环”。当前仓库的 YAML / rule 替代路线与 Part II §2.1、§7.1、§8.1、§9.1.2 冲突，且已经造成 `TRACEABILITY.md` / `STATUS.md` 对 M1 的错误完成判定。因此必须恢复 LLM-α / β / δ / ε 为 canonical 主链路，同时保留 compatibility 层以兼容现有 YAML 资产与离线测试 | FINAL |
| D-I22 | LLM Access / COMP OpenAI Gateway | strict PDF mode 下的 canonical LLM provider 固定为 COMP OpenAI API gateway；代码通过 `analyzers/llm_gateway.py` 中的 shared gateway skeleton 接入，Hydra 配置位于 `cfg/llm/comp_openai.yaml`。密钥与部署名只能通过环境变量提供：`COMP_OPENAI_API_KEY`、`COMP_OPENAI_BASE_URL`、`COMP_OPENAI_API_VERSION`、`CRE_LLM_ALPHA_DEPLOYMENT`、`CRE_LLM_BETA_DEPLOYMENT`、`CRE_LLM_DELTA_DEPLOYMENT`、`CRE_LLM_EPSILON_DEPLOYMENT`。仓库与文档不得保存真实 key | `COMP OpenAI Access Guide v3.11.pdf` 明确要求通过 `api-key` header 和 deployment-based endpoint 访问 gateway；而用户已确认 key 通过环境变量注入。将 provider / env-var / gateway skeleton 一并冻结，可避免后续各模块自行发明接入方式、把密钥写进仓库，或再次出现“文档说无需 key、架构却要求 LLM”的漂移 | FINAL |
| D-I23 | Strict PDF Path / Legacy Report-Patch Stack 降级 | `semantic_analyzer.py`、`report_generator.py`、`repair_generator.py`、`validator.py`，以及它们对应的 `SemanticReport`、`CREReport`、`RepairPatch`、`RepairResult`、`ValidationResult`，全部降级为 historical / legacy artifacts。strict PDF mode 下的 canonical 主链路固定为：`DiagReport`（M2/M5 后）→ `M4.generate_failure_hypothesis()` → `M7.generate_repair_proposals()` → `M8.evaluate_acceptance_criteria()` + `M8.verify_semantic_consistency()` → `AcceptanceVerdict` → `PO.run_cre_pipeline()` | 单靠 `D-I21` 恢复 LLM-α / β / δ / ε 还不足以阻止旧 `CREReport / patch / validator` 路线在 README、旧决策和集成说明里继续冒充主线。需要一个明确的 superseding 决策，把这些旧结构整体降级为 legacy/historical，并把 strict PDF canonical 数据流重新钉死 | FINAL |
| D-I1 | Phase 1 / Spec Validator 接口 | `validate_spec_set(reward_spec_path, constraint_spec_path, policy_spec_path, env_spec_path) -> ValidationResult` 签名冻结（4 参数版） | 实现完成，签名与实现一致 | FINAL |
| D-S4 | Phase 2 / StaticReport Schema | `StaticIssue{issue_id:str, issue_type:str, severity:str, rule_id:str, description:str, traceable_fields:list[str], evidence:dict}`<br>`StaticReport{spec_versions:dict[str, str], issues:list[StaticIssue], summary:dict, output_path:str | None}`<br>其中 `issue_id` 格式固定为 `CR-001` / `EC-001` / `ER-001`，`issue_type ∈ {"C-R","E-C","E-R"}`，`severity ∈ {"error","warning","info"}`，`summary` 至少包含 `{"total": int, "by_type": dict[str, int], "by_severity": dict[str, int]}` | Phase 2 需要稳定、可序列化、可追溯的静态报告合同供后续 Semantic / Report / Repair 阶段消费；保留 `output_path` 以同时支持 dry-run 与写盘模式；字段集合保持最小化，只承载问题、汇总和追溯信息，不在此阶段引入运行时统计 | FINAL |
| D-I2 | Phase 2 / Static Analyzer 接口 | `run_static_analysis(reward_spec_path, constraint_spec_path, policy_spec_path, env_spec_path, output_dir: str | None = None) -> StaticReport` 签名冻结（4 个 spec 路径参数版） | Phase 2 必须以 reward / constraint / policy / environment 四份 spec 作为完整输入合同；接口与 `INTERFACES.md` 冻结条目保持一致，并支持可选写盘 | FINAL |
| D-SA-rules | Phase 2 / Static Analysis Rules | `static_analyzer.py` 首版必须实现以下规则，且 `issue_id` 前缀按问题维度分配：<br>1. `type_compatibility`（C-R, warning）：若 `reward.term_expr` 与任一 hard constraint `indicator_predicate` 共享状态变量 token，且该 reward term `weight > 0`，则报告可能的 boundary-seeking 激励。理论依据：CRE_v4 第 8 页 §2.3 规则 (1) `Type compatibility`；本项目以表达式 / 谓词字符串的变量 token overlap 作为可实现的静态近似。<br>2. `domain_boundary`（C-R, warning）：若 reward term `weight > 0` 且 `clip_bounds.min >= 0`，同时与任一 hard constraint 共享变量 token，则报告 reward 支撑可能落在约束边界方向。理论依据：CRE_v4 第 8 页 §2.3 规则 (2) `Domain boundary`；本项目以非负支撑 + 变量重叠作为符号化近似。<br>3. `coverage_prebound`（E-C, warning）：对每个 hard constraint，检查 `E_tr.scene_families` 与顶层 `shift_operators.description` 是否存在可覆盖该约束 critical region 的关键词匹配；若无匹配则报告训练分布覆盖不足。理论依据：CRE_v4 第 8 页 §2.3 规则 (3) `Coverage pre-bound`，其要求每个 hard constraint 至少有一个 scene family 支撑。<br>4. `deployment_shift_coverage`（E-R, warning）：若 `E_dep.deployment_envs[*].applied_shift_operators` 存在非空项，而 `E_tr.shift_operators == []`，则报告训练分布未显式覆盖部署 shift。理论依据：CRE 的 `E_tr / E_dep` 双环境表示要求训练环境对部署偏移给出显式覆盖声明；缺失 shift coverage 是典型 environment-reward 风险。<br>5. `soft_constraint_penalty_alignment`（C-R, info）：对每个 soft constraint，若其 `penalty_weight > 0`，且存在共享变量 token 的 reward term 具有 `weight > 0`，则报告 reward 激励与 soft constraint 惩罚可能相互牵制。理论依据：CRE Class I 关注 reward 与 constraint 的方向性不一致；由于 Phase 2 不解析表达式语义，该规则仅作为低置信度 `info`。<br>统一约束：Phase 2 首版的变量共享与关键词覆盖判断统一采用 token heuristic，不做 AST / SMT / 谓词求解；因此上述规则默认输出 `warning/info`，保留 `error` 等级给未来可证明的结构性矛盾 | CRE_v4 第 8 页 §2.3 给出静态 pre-check 的三条理论约束；本项目因跳过 LLM-α 与正式符号解析，只能以字符串 token / 关键词匹配实现其可操作近似，因此需先冻结规则边界、证据来源与默认严重等级，避免后续实现随意扩大解释范围 | FINAL |
| D-S5 | Phase 3 / Legacy Log Field Mapping | 旧版运行日志采用 run 目录格式：`logs/<run_id>/manifest.json`、`summary.json`、`steps.jsonl`、`episodes.jsonl`，以及 `episodes/episode_XXXX.json`。`episodes/episode_XXXX.json` 为每 episode 一个 JSON，结构为 `{"summary": EpisodeLog, "steps": list[StepLog]}`；`episodes.jsonl` 为逐 episode 的 `EpisodeLog` 摘要流；`steps.jsonl` 为逐 step 的 `StepLog` 流。实际 schema 由 `envs/cre_logging.py` 中的 `StepLog` / `EpisodeLog` dataclass 定义，`runtime_logging/schema.py` 仅做 re-export。<br><br>字段映射表：<br>`min_obstacle_distance` -> `constraint.indicator_predicate:min_obstacle_distance_m` / `reward.term_expr:min_static_obstacle_distance_m`，映射方式：直接重命名并按 step 使用，备注：当前日志只有单一最小障碍物距离，不区分 static/dynamic。<br>`goal_distance` -> `reward.term_expr:goal_distance_t`，映射方式：step 直接使用；`goal_distance_t_minus_1` 由相邻 step 回看一帧推导，备注：首帧无前值。<br>`position[0]` / `position[1]` / `position[2]` -> `position_x_m` / `position_y_m` / `flight_height_m`，映射方式：从 step `position` 向量拆分，备注：可支撑 workspace / height 相关检查。<br>`velocity` -> `body_linear_velocity_mps`，映射方式：直接映射为 3D 线速度向量，备注：可用于 `l2_norm(body_linear_velocity_mps)`。<br>`yaw_rate` -> `body_yaw_rate_radps`，映射方式：直接重命名。<br>`collision_flag` -> `done_type == 2` / `collision_avoidance`，映射方式：布尔真时直接视作 collision witness，备注：优先级高于字符串 done_type。<br>`out_of_bounds_flag` -> `done_type == 3` / `workspace_boundary`，映射方式：布尔真时直接视作 out-of-bounds witness。<br>`done_type`（日志字符串） -> spec `done_type`（整数编码 0/1/2/3/4），映射方式：归一化字典 `{running:0, success:1, collision:2, out_of_bounds:3, truncated:4}`；额外别名 `{manual_exit:4, manual_regen:4, unknown:4}`，备注：动态分析内部统一转换为整数编码再做 spec 对齐。<br>`reward_total` -> episode / step 总回报，映射方式：直接使用，备注：只用于行为摘要，不替代 reward term 计算。<br>`reward_components[reward_progress|reward_safety_static|reward_safety_dynamic|penalty_smooth|penalty_height|manual_control]` -> 对应 `reward.reward_terms[*].term_id`，映射方式：按 term_id 同名对齐，备注：若 term_id 与标准组件键同名则优先用日志中已分解的 reward component。<br>`reward_components_total.*` -> episode 级 reward term 总和，映射方式：按同名 term_id 聚合结果对齐，备注：用于 episode summary。<br>`target_position` -> goal reference，映射方式：保留为辅助证据字段，备注：当前 v1 reward/constraint spec 未直接引用。<br>`scene_cfg_name` / `scenario_type` / `scene_tags` -> environment context，映射方式：直接读取作为部署环境标签，备注：可用于按场景族切分动态问题。<br><br>缺失字段列表与处理：<br>`min_dynamic_obstacle_distance_m`：日志缺失；若 `reward_safety_dynamic` 需要严格检测，则跳过该 reward term 的动态重算，并在报告中标记 missing-field warning。<br>`action_cmd_world_mps_t` / `action_cmd_world_mps_t_minus_1`：日志缺失；`penalty_smooth` 只能使用已记录的 `reward_components.penalty_smooth`，不能从原始动作重算。<br>`manual_override_active`：日志缺失；`manual_control` 只能使用已记录的 `reward_components.manual_control`，不能从 step 原始字段恢复。<br>`workspace_size_x_m` / `workspace_size_y_m`：日志缺失；`workspace_boundary` 动态检测优先依赖 `out_of_bounds_flag` / `done_type`，不从几何边界重建。<br>`body_roll_rad` / `body_pitch_rad`：日志缺失；`attitude_turn_rate` 只能部分检测 yaw-rate 分支，roll/pitch 分支跳过并标记 partial-coverage warning。<br><br>`done_type` 对应关系：spec 编码 `0=running, 1=success, 2=collision, 3=out_of_bounds, 4=truncated`；日志字符串分别映射为 `running->0`、`success->1`、`collision->2`、`out_of_bounds->3`、`truncated->4`。对约束的直接对应为：`collision_avoidance` 可使用 `collision_flag==True` 或 `done_type==2`；`workspace_boundary` 可使用 `out_of_bounds_flag==True` 或 `done_type==3` | Phase 3 必须先冻结“日志里到底有什么、哪些字段能直接对齐 spec、哪些只能代理或缺失”的映射边界。实际日志样本表明旧版运行目录同时存在 JSONL 流和 per-episode JSON 文件，且 `done_type` 使用字符串标签而非 D003 的整数编码，因此动态分析在实现前必须先确定读取优先级、别名归一化和缺失字段处理策略，否则不同 Agent 会对同一日志得出不同解释 | FINAL |
| D-I3 | Phase 3 / Dynamic Analyzer 接口 | `run_dynamic_analysis(static_report: StaticReport, log_dir: str, reward_spec_path: str, constraint_spec_path: str, output_dir: str | None = None) -> DynamicReport` 签名冻结；`DynamicReport{spec_versions: dict[str, str], episode_count: int, issues: list[DynamicIssue], summary: dict, output_path: str | None}`，其中 `DynamicIssue` 结构与 `StaticIssue` 相同 | Dynamic Analysis 需要同时消费 Phase 2 `StaticReport`、真实日志目录以及 reward/constraint spec；环境上下文已通过日志 manifest / scene tags 携带，因此首版接口不再重复传入 env spec 路径。`DynamicIssue` 复用 `StaticIssue` 结构可减少后续 Report/Semantic/Repair 阶段的分支复杂度 | FINAL |
| D-S6 | Phase 4 / Utility Oracle | Utility Oracle `U(π; e)` 采用 task-completion flag（`done_type == 1`）作为主信号，辅以 safety monitor（`collision_flag == False AND out_of_bounds_flag == False`）。对每条 episode，`U = 1.0` 当且仅当 `done_type == 1`，否则 `U = 0.0`。该 oracle 在 reward 设计之前确定，与 `R` 的构造统计独立（满足 CRE_v4 Assumptions 3.2–3.4） | CRE_v4 §3.6 要求 `U` 与 `R` 统计独立；`done_type == 1` 是任务完成的直接物理判定，不依赖 reward 函数任何参数；safety monitor 在 `done_type` 缺失时可降级使用 | FINAL |
| D-I4 | Historical Phase 4 / Semantic Analyzer 接口 | `run_semantic_analysis` 旧接口签名只保留为 `analyzers/legacy/semantic_analyzer.py` 的历史快照；strict PDF mode 下不再把它视为 canonical M4 接口，现由 `m4.py::generate_failure_hypothesis()` 单函数 contract 接管 | 保留该决策仅用于解释旧 `analyzers/legacy/semantic_analyzer.py` 如何偏离 PDF；当前 canonical 语义见 `D-I21` / `D-I23` | FINAL（historical / superseded by D-I23） |
| D-S7 | Historical Phase 5 / CRE Report 汇总格式 | `CREReport` 作为旧版 `analyzers/legacy/semantic_analyzer.py -> analyzers/legacy/report_generator.py` 流水线的汇总载体被保留为历史参考；strict PDF mode 下它不再是 canonical 数据结构，现已被 `DiagReport`（诊断阶段）与 `AcceptanceVerdict`（验收阶段）取代，见 `D-I21` / `D-I23` | 保留该记录仅用于解释旧 `analyzers/legacy/report_generator.py` 的设计来源；继续把 `CREReport` 当主线会再次偏离 PDF 的 canonical dataflow | FINAL（historical / superseded by D-I23） |
| D-I5 | Historical Phase 5 / Report Generator 接口 | `generate_report` 的旧接口签名只保留为 `analyzers/legacy/report_generator.py` 的历史快照；strict PDF mode 不再把它作为 canonical Phase 5 接口，现由 `repair/m7.py::generate_repair_proposals()` 接管 | 旧接口可用于审计历史偏差，但不能继续作为当前 M7 设计依据 | FINAL（historical / superseded by D-I23） |
| D-S8 | Historical Phase 6 / Repair Patch 格式 | `RepairPatch` / `RepairResult` 只保留为旧 `repair/legacy/repair_generator.py` 的 historical artifact；strict PDF mode 下 canonical Phase 5/6 输出已切换为 `RepairProposal` 与 `AcceptanceVerdict`，不得再以 patch/result 作为主结构，见 `D-I21` / `D-I23` | 保留 patch 粒度设计是为了说明旧路线如何工作，但它不符合 PDF Part II §8–§10 的 canonical module contracts | FINAL（historical / superseded by D-I23） |
| D-I6 | Historical Phase 6 / Repair Generator 接口 | `generate_repair` 旧接口签名只保留为 `repair/legacy/repair_generator.py` 的历史快照；strict PDF mode 不再把它作为 canonical 修复入口，现由 `repair/m7.py::generate_repair_proposals()` 与 `repair/m8.py::run_acceptance_loop()` 接管 | 旧接口若继续被视为当前主线，会与 `RepairProposal` / `AcceptanceVerdict` 的 canonical 路径冲突 | FINAL（historical / superseded by D-I23） |
| D-S9 | Historical Phase 7 / Validation 格式 | `ValidationResult`（`repair/legacy/validator.py`）只保留为旧 patch-validation 流程的历史记录；strict PDF mode 下 canonical acceptance 结果为 `AcceptanceVerdict`，semantic gate 由 `M8.verify_semantic_consistency()` 单独执行，见 `D-I21` / `D-I23` | 旧 patch validation 与 PDF 的 acceptance protocol 并不等价；保留该记录仅用于历史偏差审计 | FINAL（historical / superseded by D-I23） |
| D-I7 | Historical Phase 7 / Validator 接口 | `validate_repair` 旧接口签名只保留为历史快照；strict PDF mode 不再将其视为 canonical 验收接口 | 继续保留该决策的唯一目的，是帮助定位 legacy 路线如何偏离 PDF | FINAL（historical / superseded by D-I23） |
| D-S10 | Historical Phase 8 / Integration 接入策略 | `train.py` 末尾追加 `run_static_analysis -> run_dynamic_analysis -> run_semantic_analysis -> generate_report -> generate_repair -> validate_repair` 的旧链路，只保留为 historical integration artifact；strict PDF mode 下 canonical 顶层入口已切换为 `pipeline/orchestrator.py::run_cre_pipeline()` | 原策略服务的是 pre-refactor 旧流水线，与 PDF Part II §10.1.1 的模块顺序和数据流不一致；保留它只是为了说明 legacy hook 的来源 | FINAL（historical / superseded by D-I23） |
| D-I8 | Historical Phase 8 / Integration 接口 | `train.py` 追加调用版接口只保留为历史快照；strict PDF mode 下 canonical integration surface 是 `pipeline/orchestrator.py` 与 strict `cfg/cre.yaml` | 把旧集成接口继续标成当前 FINAL 会误导后续实现回写到 `train.py` 附加链路 | FINAL（historical / superseded by D-I23） |
| D-S11 | Phase 9 / Benchmark Suite 格式 | Benchmark suite 位于 `cfg/benchmark_cfg/`，包含四个子目录：`clean_nominal/`（干净 spec，期望 PASS）、`injected_cr/`（注入 C-R 冲突）、`injected_ec/`（注入 E-C 不匹配）、`injected_er/`（注入 E-R 不匹配）。每个子目录包含四份 spec 文件，采用受控 fixture：`clean_nominal` 为最小干净 spec（无任何静态冲突），`injected_cr` / `injected_ec` / `injected_er` 为手工构造的最小触发 case，确保各自能稳定触发对应检测规则。Benchmark 运行脚本为 `scripts/run_benchmark.py`，对四个 case 各跑一次完整静态分析，输出 `benchmark_results.json`，格式为 `{case_name: {\"alarm\": bool, \"psi_cre\": float, \"total_issues\": int}}` | Phase 9 需要可复现的注入测试验证检测器灵敏度；受控 fixture 比“直接复制线上 v1 spec 后微调”更稳定，也避免基线 spec 自身携带无关注入的静态冲突；`clean_nominal` 作为阴性对照 | FINAL |
| D-I9 | Phase 9 / Benchmark 接口 | `run_benchmark(benchmark_dir, output_dir) -> dict` 签名冻结（见 `INTERFACES.md` Phase 9 条目） | 接口设计完成 | FINAL |
| D-BM1 | Phase 9 / Benchmark injected_er 偏差 | `injected_er` case 当前只能检测到 `total_issues > 0`（通过 `SEM-ER-DETECTION` 规则），无法触发 `alarm=True`。Phase 9 DoD 中“`injected_er -> acceptance FAIL`”的要求在当前实现下无法满足，原因是 Decision A 将 `phi_er` 固定为 `None`，E-R 维度不参与 `Ψ_CRE` 计算，因此无法通过 alarm 阈值触发。该偏差已知且可接受：`SEM-ER-DETECTION` 规则确保 E-R 注入缺陷仍被检测到（`total_issues > 0`），只是不触发 alarm。若未来实现 `phi_er`（需真实多环境部署数据），该偏差自动消除 | Decision A 由用户明确选择；`phi_er` 的正确计算需要跨多个部署环境的 Pearson 相关，单次运行数据不足 | FINAL |
