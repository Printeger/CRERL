scene_id: "demo3_er_shifted_gate_base"
scene_family: "nominal"
seed: 3301

workspace:
  size_x: 14.0
  size_y: 10.0
  size_z: 3.0
  flight_height_min: 0.8
  flight_height_max: 1.6

primitive_budget:
  box: [2, 4]
  cylinder: [0, 0]
  slab: [0, 0]
  perforated_slab: [0, 0]
  sphere: [0, 0]
  capsule: [0, 0]

distribution_modes:
  background: "fixed_demo_layout"
  structure: "template_driven"
  route_adjacent_bias: 0.26
  boundary_adjacent_bias: 0.08

background_placement:
  obstacle_obstacle_min_dist: 0.30
  obstacle_boundary_min_dist: 0.24
  max_rejection_trials: 1
  free_space_fraction_min: 0.40
  free_space_fraction_max: 0.72

templates:
  enabled: true
  min_templates_per_scene: 1
  max_templates_per_scene: 1
  candidate_types:
    - bottleneck
    - low_clearance_passage

dynamic_obstacles:
  enabled: false
  max_dynamic_count: 0

start_goal:
  start_sampling_mode: "fixed_demo_anchor"
  start_band_min: 0.0
  start_band_max: 0.0
  start_clearance_min: 0.35
  goal_sampling_mode: "fixed_demo_anchor"
  goal_clearance_min: 0.35
  goal_boundary_clearance_min: 0.35
  start_goal_distance_min: 9.0
  start_goal_distance_max: 12.5
  heading_random: false

validation:
  enforce_connectivity: true
  allow_failure_analysis_only: false
  max_overlap_tolerance: 0.0
  require_template_presence: false
  require_start_goal_valid: true
  require_traversable_perforation: false
  validate_dynamic_spawn: false

logging:
  save_scene_json: true
  save_validation_report: true
  save_scene_stats: true
